---
import { ClientRouter } from 'astro:transitions';
import SEOMetadata from '../components/SEOMetadata.astro';
import FloatingWhatsApp from '../components/FloatingWhatsApp.astro';
import { config } from '../config';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  image?: string;
  type?: 'website' | 'article';
  publishedTime?: string;
  tags?: string[];
  canonical?: string;
}

const {
  title,
  description = 'Nicho Digital diseña webs premium para profesionales y negocios de servicios.',
  image,
  type = 'website',
  publishedTime,
  tags,
  canonical,
} = Astro.props;

const gaTrackingId = import.meta.env.GA_TRACKING_ID || process.env.GA_TRACKING_ID;

const siteUrl = Astro.site?.toString().replace(/\/$/, '') || '';
const defaultImage = `${siteUrl}/images/hero/optimized/nicho-digital-hero-device-system-1280.png`;
const structuredImage = image
  ? image.startsWith('http') ? image : `${siteUrl}${image}`
  : defaultImage;
const canonicalUrl = canonical || `${siteUrl}${Astro.url.pathname}`;
const organizationStructuredData = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: config.app.name,
  url: siteUrl,
  description,
};
const pageStructuredData = {
  '@context': 'https://schema.org',
  '@type': type === 'article' ? 'Article' : 'WebPage',
  headline: title,
  description,
  image: structuredImage,
  url: canonicalUrl,
  ...(publishedTime ? { datePublished: publishedTime } : {}),
};
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/favicon/favicon.svg" type="image/svg+xml">
    <link rel="alternate icon" href="/favicon/favicon.ico" sizes="any">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="apple-touch-icon" href="/favicon/apple-touch-icon.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <link
      rel="preload"
      as="image"
      href="/images/hero/optimized/nicho-digital-hero-device-system-1280.avif"
      imagesrcset="/images/hero/optimized/nicho-digital-hero-device-system-512.avif 512w, /images/hero/optimized/nicho-digital-hero-device-system-1024.avif 1024w, /images/hero/optimized/nicho-digital-hero-device-system-1280.avif 1280w"
      imagesizes="100vw"
      type="image/avif"
      fetchpriority="high"
    >
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <SEOMetadata title={title} description={description} image={image} type={type} publishedTime={publishedTime} tags={tags} canonical={canonical} />
    <script type="application/ld+json" set:html={JSON.stringify(organizationStructuredData)} />
    <script type="application/ld+json" set:html={JSON.stringify(pageStructuredData)} />
    <meta name="theme-color" content="#2563eb" />
    <ClientRouter />
    <style is:global>
      html { @apply transition-all duration-150; }
      body { @apply bg-white text-gray-900 transition-all duration-300 dark:bg-gray-900 dark:text-white; }
    </style>
    <script>
      const theme = (() => {
        if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
          const storedTheme = localStorage.getItem('theme');
          if (storedTheme === 'light' || storedTheme === 'dark') return storedTheme;
        }
        if (window.matchMedia('(prefers-color-scheme: dark)').matches) return 'dark';
        return 'light';
      })();
      if (theme === 'dark') document.documentElement.classList.add('dark');
      window.localStorage.setItem('theme', theme);
      document.addEventListener('astro:after-swap', () => {
        if (localStorage.getItem('theme') === 'dark') document.documentElement.classList.add('dark');
      });
    </script>
  </head>
  <body>
    <slot />
    <FloatingWhatsApp />
    {gaTrackingId && (
      <script is:inline define:vars={{ gaTrackingId }}>
        window.addEventListener('load', function() {
          setTimeout(function() {
            const gtmScript = document.createElement('script');
            gtmScript.async = true;
            gtmScript.src = 'https://www.googletagmanager.com/gtag/js?id=' + gaTrackingId;
            document.head.appendChild(gtmScript);
            gtmScript.onload = function() {
              window.dataLayer = window.dataLayer || [];
              function gtag(){dataLayer.push(arguments);}
              gtag('js', new Date());
              gtag('config', gaTrackingId);
            };
          }, 1000);
        });
      </script>
    )}
  </body>
</html>
